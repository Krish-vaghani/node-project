const {signupSchema,loginSchema, emailSchema, passwordSchema, otpmatch} = require('./studentvalidation');

module.exports = {
    signupSchema,
    loginSchema,
    emailSchema,
    passwordSchema,
    otpmatch
}